const amount = 12;

if(amount<10) {
    console.log('small number');
}
else {
    console.log('large number');
}

console.log(`hey its my first node app!!!`);

// To call this file you need to open the VS terminal and type 'node app.js' (or 'node' +the filename)
// or open the mac terminal and type 'cd' + the directory to the file